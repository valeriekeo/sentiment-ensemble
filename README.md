# sentiment-ensemble
Multi-model sentiment ensemble that runs three different approaches on the same social media data, analyzes where they agree and disagree, extracts named entities from high-priority posts, and benchmarks GPU vs CPU inference throughput.
